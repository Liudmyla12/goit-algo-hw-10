# goit-algo-hw-10

Домашнє завдання до теми **“Лінійне програмування та рандомізовані алгоритми”**.

Репозиторій містить **2 незалежні завдання**:

1. **Лінійне програмування**: оптимізація виробництва (PuLP)
2. **Рандомізовані алгоритми**: обчислення інтеграла методом Монте-Карло + перевірка через `scipy.integrate.quad`

---

## 📌 Структура репозиторію

```text
goit-algo-hw-10/
├── task_1.py            # PuLP: оптимізація виробництва (LP)
├── task_2.py            # Monte Carlo: інтеграл + порівняння з quad
├── requirements.txt     # Залежності
├── integral_plot.png    # Графік інтеграла (результат Task 2)
├── README.md
└── .gitignore
```

---

## 🧰 Встановлення та запуск

Рекомендовано використовувати віртуальне середовище.

### Windows PowerShell

```bash
py -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -r requirements.txt
```

---

## ✅ Task 1 — Оптимізація виробництва (PuLP)

### Умова

Виробляємо 2 продукти: **Lemonade** і **Fruit Juice**.
Потрібно **максимізувати загальну кількість продуктів**, враховуючи обмежені ресурси:

- Water = 100
- Sugar = 50
- Lemon Juice = 30
- Fruit Puree = 40

Витрати ресурсів на 1 одиницю:

- Lemonade: 2 Water, 1 Sugar, 1 Lemon Juice
- Fruit Juice: 1 Water, 2 Fruit Puree

### Запуск

```bash
py .\task_1.py
```

### Результат (факт запуску)

```text
Status: Optimal
Lemonade     : 30
Fruit Juice  : 20
TOTAL        : 50
```

### Пояснення

- **Lemonade** обмежений ресурсом **Lemon Juice**: `lemonade ≤ 30`, тому максимум **30**.
- **Fruit Juice** обмежений **Fruit Puree**: `2 * juice ≤ 40`, тому максимум **20**.
- Перевірка води: `2*30 + 1*20 = 80 ≤ 100`, тобто вода **не є вузьким місцем**.
- Отже, оптимум: **30 + 20 = 50**.

---

## ✅ Task 2 — Інтеграл методом Монте-Карло + перевірка (SciPy quad)

### Функція та інтервал

Обрано: `f(x) = x^2`
Межі інтегрування: `[0, 2]`

### Запуск

Без графіка:

```bash
py .\task_2.py
```

З графіком (збережеться `integral_plot.png`):

```bash
py .\task_2.py --plot
```

### Результат (факт запуску)

Параметри: `samples = 200000`, `seed = 42`

```text
Monte Carlo estimate : 2.6664545223
Reference (scipy.integrate.quad): 2.6666666667
Absolute error       : 0.0002121444
Plot saved: integral_plot.png
```

### Висновки щодо точності

- Оцінка Монте-Карло **дуже близька** до еталонного значення `quad`.
- Абсолютна похибка ≈ **2.12e-4** для **200k** точок — нормальна для стохастичного методу.
- При збільшенні `samples` похибка в середньому зменшується приблизно як **1/√N**.

---

## ✅ Вимоги

- Python **3.10+** (підходить також 3.12)
- Залежності: **PuLP, NumPy, Matplotlib, SciPy** (встановлюються з `requirements.txt`)
